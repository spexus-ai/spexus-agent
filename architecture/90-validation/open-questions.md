# Open Questions

## Tooling

- Should the repo-local `codesearch` Hugging Face cache environment be wrapped in a reusable helper for future incremental runs?

## Architecture

- Should future iterations model a direct Spexus API/MCP integration if that control-plane surface lands in code?

## Scope

- If this repository grows a hosted control plane later, should it remain one service with extra containers, or split into separate repositories/services?
